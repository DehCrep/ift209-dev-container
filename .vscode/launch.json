{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(QEMU + gdb) debug current file",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}${pathSeparator}${fileBasenameNoExtension}",
            "cwd": "${fileDirname}",
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb-multiarch",
            "miDebuggerServerAddress": "${env:DB_HOST}:${env:DB_PORT}",
            "preLaunchTask": "(QEMU + gdbstub) debug current file",
            "stopAtEntry": true,
            "setupCommands": [
                {
                    "description": "Set breakpoint pending on",
                    "text": "set breakpoint pending on",
                    "ignoreFailures": false
                }
            ]
        }
    ]
}
